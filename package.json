{
  "name": "citysignal-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "clean": "rimraf src/.observablehq/cache",
    "prebuild": "node scripts/clear-data-cache.mjs",
    "predev": "node scripts/clear-data-cache.mjs"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0",
    "d3": "^7.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
